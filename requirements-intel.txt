# Tangier Intel (intel/) runtime dependencies — kept separate from the ML bot requirements.txt
httpx>=0.27
PyYAML>=6.0
# optional fast keccak (pure-Python fallback is built in)
pycryptodome>=3.20
# transaction signing. The reference implementation is used deliberately rather than a
# hand-rolled ECDSA: this signs transactions that move real money.
eth-account>=0.13
# tests
pytest>=8.0
pytest-asyncio>=0.23
